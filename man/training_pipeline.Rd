% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{training_pipeline}
\alias{training_pipeline}
\title{Train and Evaluate Poisson Model on Data Splits}
\usage{
training_pipeline(data_splits)
}
\arguments{
\item{data_splits}{A list containing `train_data`, `val_data`, and `test_data` data frames.}
}
\value{
The final model trained on the full dataset (train + validation + test).
}
\description{
This function runs a training pipeline for the Poisson regression model. It trains the model, evaluates performance on training and validation data,
then tests the model on the test set. A final model is trained on the full dataset (train + validation + test).
}
\examples{
\dontrun{
# Example usage:
final_model <- training_pipeline(data_splits)
}
}
