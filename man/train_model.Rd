% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{train_model}
\alias{train_model}
\title{Train a Model on Training and Validation Data and Evaluate on Test Data}
\usage{
train_model(
  data_splits,
  model_fn,
  evaluate_test = FALSE,
  train_on_full = FALSE
)
}
\arguments{
\item{data_splits}{A list containing `train_data`, `val_data`, and `test_data` data frames.}

\item{model_fn}{A function that takes a data frame and returns model results.}

\item{evaluate_test}{A logical flag indicating whether to evaluate the model on the test data. Default is FALSE.}

\item{train_on_full}{A logical flag indicating whether to train on the full dataset (train + validation + test). Default is FALSE.}
}
\value{
A list containing:
  - `train_results`: The results from training (output of `model_fn` on training data).
  - `test_results`: The results from testing (output of `model_fn` on test data), or NULL if `evaluate_test` is FALSE.
}
\description{
This function trains a model using specified training and validation data, with optional evaluation on test data.
It allows for the use of the full dataset for training if `train_on_full` is set to TRUE.
}
\examples{
\dontrun{
# Example usage:
result <- train_model(data_splits, model_fn, evaluate_test = TRUE)
training_results <- result$train_results
test_results <- result$test_results
}
}
